# Task Generator (V0 Stub)

Minimal CLI that reads `input.json` and writes `out/tasks.json` **without** any AI calls.

## How to run
```bash
# Windows PowerShell (example)
python apps/task-generator/main.py
